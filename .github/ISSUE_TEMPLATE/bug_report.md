---
name: Bug Report
about: Report a bug in MuxCls
labels: bug
---

## Description
A clear description of the bug.

## Steps to reproduce
1. ...
2. ...

## Expected behavior
What should happen.

## Actual behavior
What actually happens.

## Environment
- OS: Windows version
- Python: output of `python --version`
- FFmpeg: output of `ffmpeg -version` (first line)
- MuxCls version: (see `CHANGELOG.md` / top of README)
- Launcher used: `run.ps1` / `MuxCls` command / `python MuxCls.py`

## Input media (if relevant)
- Container/codecs and stream layout (audio/subtitle counts and languages).
- Do NOT attach copyrighted media. A minimal sample or `ffprobe` output is enough.

## Logs (if available)
Attach the relevant file from the local `Logs/` folder, with any private paths removed.

```
Paste safe log output here (no secrets or private paths).
```
